<rxui:ReactiveUserControl
    x:Class="AE.PID.Views.VersionUpdatePromptView"
    x:TypeArguments="vms:VersionUpdatePromptViewModel"
    xmlns:rxui="http://reactiveui.net"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vms="clr-namespace:AE.PID.ViewModels"
    mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Margin="8 8 8 4" Grid.Row="0"
                   FontSize="16" Text="发现可用更新" />
        <TextBlock x:Name="Description" Margin="8 4 8 8" Grid.Row="1" />
        <StackPanel Orientation="Horizontal" Grid.Row="3">
            <Button x:Name="UpdateButton" Margin="8 8 4 4" Width="64" Content="确定" />
            <Button x:Name="NotNowButton" Margin="4 8 8 4" Width="64" Content="取消" />
        </StackPanel>
    </Grid>
</rxui:ReactiveUserControl>